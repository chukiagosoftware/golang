# golang
Golang Sandbox for Learning Go 2nd Edition and Codesignal Go courses

Use ` go run main.go`  and `go run runes.go` for testing. However, running `go build` will fail since both contain a "main" package. 

This repo is just for quick tests following along the book and codesignal Go courses.

