# golang
Golang Sandbox for Learning Go 2nd Edition

Use ` go run main.go`  and `go run runes.go` for testing. However, running `go build` will fail since both contain a "main" package. This is just for quick tests following along the book.

